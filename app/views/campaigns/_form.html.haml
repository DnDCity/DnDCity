= simple_form_for(@campaign) do |f|
  = f.error_notification
  .form-inputs
    .main.row
    .sectioncamp.col-md-2
      = f.input :name
      = f.input :desc
      .form-actions
        = f.button :submit
