%h1 Listing characters

%table
  %tr
    %th Name
    %th User
    %th Base str
    %th Base dex
    %th Base con
    %th Base int
    %th Base wis
    %th Base cha
    %th
    %th
    %th

  - @characters.each do |character|
    %tr
      %td= character.name
      %td= character.user
      %td= character.base_str
      %td= character.base_dex
      %td= character.base_con
      %td= character.base_int
      %td= character.base_wis
      %td= character.base_cha
      %td= link_to 'Show', character
      %td= link_to 'Edit', edit_character_path(character)
      %td= link_to 'Destroy', character, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Character', new_character_path
